[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
cmds = [
  "pnpm --filter @metriva/api exec prisma generate",
  "pnpm --filter @metriva/api run build"
]

[start]
cmd = "node dist/main.js"
